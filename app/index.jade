doctype html
html(lang='en')
    head
        title Angular 2 Seed Project Example
        meta(charset="utf-8")
        meta(http-equiv="X-UA-Compatible",  content="IE=edge")
        meta(name="description", content="Angular 2 ES6 Starter")
        meta(name="viewport", content="width=device-width, initial-scale=1")
    body
        header
        main
            example-component(example-directive="Hello Example Directive" ) Loading Your First Component
        footer
        <!-- @if DEV -->
        script(src='/jspm_packages/system.js')
        script(src='/config/jspm.config.js')
        script.
            System.import('boot').catch(console.log.bind(console));
        <!-- @endif -->
        <!-- @if PROD -->
        script(src='ng2Seed.sfx.min.js')
        <!-- @endif -->

